<p id="notice"><%= notice %></p>

<div class="float-right">
  <%= link_to t('helpers.submit.create', 
        model: t('activerecord.models.video.one')), 
        new_admin_video_path, class: 'btn btn-primary' %>
</div>

<h1>
  <span class="bd-content-title">
    <%= t('activerecord.models.video.other') %>
  </span>
</h1>


<table class="table">
  <thead>
    <tr>
      <th scope="col"><%= t '.title' %></th>
      <th scope="col"><%= t '.user' %></th>
      <th scope="col"><%= t '.created_at' %></th>
      <th scope="col"></th>
    </tr>
  </thead>

  <tbody>
    <% @admin_videos.each do |video| %>
      <tr>
        <td><%= video.title %></td>
        <td><%= video.user.name %></td>
        <td><%= video.created_at %></td>
        <td>
          <%= link_to t('helpers.actions.show'), video, class: 'btn btn-sm btn-info' %>
          <%= link_to t('helpers.actions.edit'), edit_admin_video_path(video), class: 'btn btn-sm btn-success' %>
          <%= link_to t('helpers.actions.destroy'), video, method: :delete, class: 'btn btn-sm btn-danger', 
            data: { confirm: t('helpers.confirm.destroy') } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>


